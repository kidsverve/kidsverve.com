# Add painting images here

Put painting image files (`.jpg`, `.png`, `.webp`) in this folder, then reference them
from `data/paintings.json` using the path `assets/paintings/your-file.jpg`.

If an image is missing, the site shows a friendly coloured placeholder automatically.
