# Add drawing images here

Put drawing image files (`.jpg`, `.png`, `.webp`) in this folder, then reference them
from `data/drawings.json` using the path `assets/drawings/your-file.jpg`.

If an image is missing, the site shows a friendly coloured placeholder automatically.
